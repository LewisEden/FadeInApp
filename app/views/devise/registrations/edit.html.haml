%br
.col-md-12
  .card.card-space
    .card-block
      .form-header.primary-color
        %h3
          %i.fa.fa-user
          Edit User:
      = simple_form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f|
        = f.error_notification
        .form-inputs
          .md-form
            %i.fa.fa-envelope.prefix
            = f.input_field :email, id: "email", required: true, autofocus: true, :class => "form-control"
            %label{:for => "email"} Email
          .md-form
            %i.fa.fa-user.prefix
            = f.input_field :user_name, id: "username", required: true, :class => "form-control"
            %label{:for => "username"} Username
          - if devise_mapping.confirmable? && resource.pending_reconfirmation?
            %p
              Currently waiting confirmation for: #{resource.unconfirmed_email}
          .md-form
            %i.fa.fa-lock.prefix
            = f.input_field :password, id: "password", required: true, :class => "form-control"
            %label{:for => "password"} New password(leave blank if you don't wish to change it)
          .md-form
            %i.fa.fa-lock.prefix
            = f.input_field :password_confirmation, id: "password_confirmation", required: false, :class => "form-control"
            %label{:for => "password_confirmation"} Confirm password
          %hr
          .md-form
            %i.fa.fa-lock.prefix
            = f.input_field :current_password, id: "current_password", required: false, :class => "form-control"
            %label{:for => "current_password"} Current password (needed to confirm changes to your account)
          Website Theme
          = f.input :theme, label: false, prompt: "Select website theme", collection: ["Default", "Black", "Indigo", "Cyan", "Purple", "Navy", "Pink", "Light blue", "Grey"], input_html: { class: "browser-default" }
        .form-actions
          %button.btn.btn-primary.primary-color.btn-rounded Update
      .list-group-item.col-md-4.float-right.list-group-item-danger
        %p
          Unhappy? #{link_to "Cancel my account", registration_path(resource_name), data: { confirm: "Are you sure?" }, method: :delete, class: "btn btn-danger btn-rounded btn-sm"}
      = link_to "Back", :back, :class => "btn btn-secondary btn-rounded btn-sm"
